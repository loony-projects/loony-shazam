<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-build-only network security config. Since targetSdk >= 28, Android
  blocks cleartext (plain HTTP) traffic by default — which would silently
  break the debug build's connection to a local dev backend at
  http://10.0.2.2:8080/ (see docs/android.md "Pointing the debug build at
  a local backend"). This override permits cleartext traffic ONLY to the
  emulator's host-loopback alias and localhost, and ONLY in debug builds
  (this file lives under src/debug/, not src/main/ — release builds never
  see it and keep the platform default of requiring HTTPS everywhere).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
