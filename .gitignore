# Environment / secrets
.env
*.env.local

# Rust
/backend/target/
**/*.rs.bk

# Python
processor/.venv/
**/__pycache__/
processor/**/*.pyc
processor/.pytest_cache/
processor/.ruff_cache/
processor/.mypy_cache/
processor/**/*.egg-info/
processor/build/
processor/dist/

# Android / Gradle
android/.gradle/
android/**/build/
android/local.properties
android/.idea/
android/*.iml
android/captures/
android/.cxx/

# OS / editor
.DS_Store
*.swp
.vscode/
.idea/

# Test/scratch audio (never commit real music — testdata/audio is
# intentionally empty in version control, see docs/ingestion.md; anything
# generated into it locally, e.g. via scripts/dev/generate_test_catalog.py,
# stays untracked)
/testdata/audio/*
!/testdata/audio/README.md

# Local (non-Docker) run state: pid files and logs from
# scripts/dev/run_local.sh
/.run/

# Extracted cover art (regenerated from your own audio files at ingestion
# time — never something to commit, see ARTWORK_DIR in .env.example)
/data/artwork/

# Misc
*.log
